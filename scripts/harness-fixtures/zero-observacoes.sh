#!/usr/bin/env bash
# Corre, encontra o alvo, e não observa propriedade nenhuma.
#
# Um filtro que não bateu, uma suite vazia, um selector que deixou de existir:
# o resultado é o mesmo e não é sucesso.
echo "Equivalência de valores renderizados:"
echo "  declarações comparadas: 0"
exit 0
